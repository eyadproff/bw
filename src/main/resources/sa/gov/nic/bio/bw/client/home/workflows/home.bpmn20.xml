<?xml version="1.0" encoding="UTF-8"?>
<definitions id="definitions"
             targetNamespace="http://activiti.org/bpmn20"
             xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:activiti="http://activiti.org/bpmn">

    <signal id="menuSignal" name="The Signal"/>

    <process id="homeProcess" name="Home Process">

        <intermediateCatchEvent id="catchSignalEvent">
            <signalEventDefinition signalRef="menuSignal" />
        </intermediateCatchEvent>

        <startEvent id="startEvent"/>

        <sequenceFlow sourceRef="startEvent" targetRef="menuChoosing"/>

        <userTask id="menuChoosing" activiti:formKey="sa.gov.nic.bio.bw.client.home.HomePaneFxController">
            <extensionElements>
                <activiti:formProperty id="menuId" type="string"/>
                <activiti:formProperty id="logoutRequested" type="boolean"/>
            </extensionElements>
        </userTask>

        <sequenceFlow sourceRef="menuChoosing" targetRef="endEvent"/>

        <endEvent id="endEvent"/>

    </process>

</definitions>